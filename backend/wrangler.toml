name = "taiz-directory"
main = "index.ts"
compatibility_date = "2024-09-23"

[[d1_databases]]
binding = "DB"
database_name = "taiz-directory-db"
# Placeholder -- no Cloudflare account is wired up in this environment yet.
# Before deploying for real: `wrangler d1 create taiz-directory-db` and
# paste the returned database_id here. Local dev (`--local` flag) does not
# need a real id.
database_id = "00000000-0000-0000-0000-000000000000"
